spring:
    application:
        name: demo
    datasource:
        url: jdbc:postgresql://localhost:5440/postgres
        username: postgres
        password: postgres
        driver-class-name: org.postgresql.Driver
#logging:
#    level:
#        root: ERROR